// Removed TypeScript interfaces and type definitions
// Constants and enums are kept

// Template type options
// Previously: export type TemplateType = "modern" | "classic";
// In JSX we use string constants or documentation comments

/**
 * Student Data object structure
 * Properties:
 * - studentName: string
 * - rollNumber: string
 * - class: string
 * - division: string
 * - photo: string (optional)
 * - allergies: array of strings (optional)
 * - rackNumber: string
 * - busRoute: string
 * - template: "modern" | "classic"
 * - timestamp: string
 */